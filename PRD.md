# 雅思词汇连连看 产品需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 产品名称 | 雅思词汇连连看（IELTS Word Match） |
| 文档版本 | v1.1（词库补全 + 游戏化激励规划） |
| 更新日期 | 2026-09-22 |
| 文档状态 | 现状基线 + 迭代规划 |
| 产品形态 | 纯静态 Web 小游戏（HTML + CSS + 原生 JavaScript） |

---

## 1. 产品概述

### 1.1 产品背景

雅思考生需要长期、反复地记忆 8000 量级的核心词汇，传统背单词方式（词书、App 刷词）形式单一、缺乏即时反馈与成就感。本产品将"英文单词 ↔ 中文释义"的配对记忆过程游戏化，用连连看玩法降低背词枯燥感，并通过首字母定向练习、错词/熟词混排、学习数据沉淀三个机制提升记忆效率。

### 1.2 产品定位

一款**零安装、离线可用、面向雅思考生的轻量词汇记忆练习工具**。用户双击网页文件即可使用，所有学习数据保存在本地浏览器中，不依赖服务器与账号体系。

### 1.3 产品目标

| 优先级 | 目标 | 衡量方式 |
|---|---|---|
| P0 | 完成"单词—释义"配对核心玩法闭环 | 可完整进行一局选字母→配对→结算的游戏 |
| P0 | 支持按首字母定向练习薄弱字母 | 选择字母后该局词汇以该字母单词为主 |
| P1 | 沉淀学习数据，提供可视化进度反馈 | 用户中心可查看总体/分字母掌握进度 |
| P1 | 支持一台设备多人使用（如家庭、自习室共用电脑） | 本地多用户档案，最多 3 人 |
| P2 | 提升重复记忆效率 | 抽题时混合已掌握词与新词 |

### 1.4 非目标（本期不做）

- 不做在线账号、云端同步、排行榜、社交分享。
- 不做真人对战、道具商城、广告变现。
- 不做服务端词表更新与 AI 出题，词表以本地数据文件为准。

---

## 2. 目标用户与使用场景

### 2.1 目标用户

- 备考雅思、需要系统扩充词汇量的学生（大学生为主）。
- 习惯用碎片时间（课间、通勤、睡前 2–5 分钟）进行轻量练习的用户。
- 共用一台电脑学习的多名用户（本地切换用户名即可）。

### 2.2 核心使用场景

1. **碎片速刷**：不选字母直接开始，90 秒内尽量多配对，利用课间完成一局。
2. **专项攻坚**：发现自己 C 字母词汇薄弱 → 选 C → 连续开局，集中强化该字母单词。
3. **进度回顾**：打开"我的"页面查看总体掌握百分比与各字母进度，决定下一局练哪个字母。

---

## 3. 产品结构（信息架构）

```
雅思词汇连连看
├── 游戏页（index.html）
│   ├── 顶部导航：游戏 / 我的
│   ├── 字母选择页（默认页）
│   │   ├── 26 个字母选择器（A–Z，单选）
│   │   └── 开始游戏按钮
│   └── 对局页
│       ├── 得分显示
│       ├── 倒计时圆环（90s）
│       ├── 30 张卡片的配对区域（英文单词 ×15 / 中文释义 ×15）
│       ├── 控制按钮：重新选择字母 / 重新开始 / 下一关卡
│       └── 提示信息条
└── 我的页（profile.html）
    ├── 个人信息（用户名、已掌握词数、已完成关卡、游戏次数、清除数据）
    ├── 学习统计
    │   ├── 总体掌握进度条（已掌握数 / 词库总数）
    │   └── 分首字母进度（A–Z 分组计数 + 进度条）
    └── 所有用户列表（关卡、词汇数、清除数据）
```

---

## 4. 核心功能需求

### 4.1 用户建档与本地多用户（P0）

| 编号 | 需求 | 说明 |
|---|---|---|
| U-01 | 首次进入弹窗输入用户名 | 使用浏览器 `prompt` 收集；不允许空名（取消/留空则重新询问） |
| U-02 | 自动建立用户档案 | 档案包含：用户名、累计配对词数、已完成关卡数、已掌握单词列表、游戏历史 |
| U-03 | 记忆上次用户 | 当前用户名存入 localStorage，再次进入自动识别 |
| U-04 | 多用户上限 | 本地最多保留 3 个用户档案，超出时保留最新的 3 个 |
| U-05 | 清除用户数据 | "我的"页可删除任一用户档案，操作前二次确认；删除当前用户后清除当前用户标记 |

### 4.2 字母选择（P0）

| 编号 | 需求 | 说明 |
|---|---|---|
| L-01 | 展示 A–Z 共 26 个字母圆形按钮 | 单选；选中态高亮，再次点击其他字母切换 |
| L-02 | 可不选字母直接开始 | 不选时从全词库随机抽词 |
| L-03 | 按首字母过滤词汇 | 选择字母后，该局只抽以该字母（不区分大小写）开头的单词 |
| L-04 | 词量不足自动补词 | 若该字母单词不足 15 个，用其他字母的随机词补足至 15 个，保证可开局 |
| L-05 | 零词字母拦截 | 若该字母下 0 个单词，进入对局页但不出牌，提示"词汇表中没有以 X 开头的单词，请选择其他字母" |
| L-06 | 返回重选 | 对局中点击"重新选择字母"回到字母页，取消选中状态并清空提示 |

### 4.3 核心对局玩法（P0）

| 编号 | 需求 | 说明 |
|---|---|---|
| G-01 | 每局 15 对词汇 | 共 30 张卡片：15 张英文单词 + 15 张对应中文释义 |
| G-02 | 卡片随机布局 | 开局在 6×5 / 5×6 / 4×8 三种网格布局中随机选用一种 |
| G-03 | 卡片随机顺序 | 单词卡与释义卡全部洗牌后放入网格 |
| G-04 | 点击翻面选中 | 依次点击两张卡片；选中卡片放大高亮，最多同时选 2 张 |
| G-05 | 配对判定 | 两张卡片属于同一单词且类型不同（一张单词、一张释义）即配对成功 |
| G-06 | 配对成功反馈 | 两张卡片置为紫色匹配态、缩小、不可再点；得分 +10；该词记入"已掌握单词" |
| G-07 | 配对失败反馈 | 短暂停留（约 0.5s）后自动取消两张卡片选中态，不扣分 |
| G-08 | 已配对/已选卡片防重复点击 | 已匹配、已选中状态的卡片不可重复操作 |
| G-09 | 通关庆祝 | 30 张卡片全部消除时播放简易烟花粒子特效 |

### 4.4 计时、计分与关卡（P0）

| 编号 | 需求 | 说明 |
|---|---|---|
| T-01 | 单局时长 90 秒 | 右上角圆环倒计时，逐秒减少，圆环随时间消减 |
| T-02 | 时间到自动结算 | 倒计时归零立即结束本局，禁止继续操作 |
| T-03 | 计分规则 | 每成功配对 1 对得 10 分，单局满分 150 分 |
| T-04 | 关卡总数 10 关 | 用户档案记录已完成关卡数，进入时定位到下一关 |
| T-05 | 通关条件 | 90 秒内消除全部 15 对卡片才算通关，通关后已完成关卡 +1，出现"下一关卡"按钮 |
| T-06 | 通关全部 10 关 | 关卡计数重置回第 1 关，可循环游玩 |
| T-07 | 重新开始 | 点击"重新开始"重置计时与得分，**不重新抽词**，仅重新洗牌发牌 |
| T-08 | 下一关卡 | 按当前字母/随机规则重新抽词并开始新一局 |

> 注：当前代码中"通关需配对 ≥10 对"的分支只在全部消除时触发，实际通关门槛等同于"15 对全消除"。该判定冗余，列入后续优化项。

### 4.5 抽题与记忆策略（P1）

| 编号 | 需求 | 说明 |
|---|---|---|
| W-01 | 老词新词混合 | 一局 15 词中，优先纳入最多 5 个已掌握单词（复习）+ 最多 10 个未掌握单词（新词） |
| W-02 | 词量不足兜底 | 混合后不足 15 个时，从当前候选词池剩余词中顺序补足 |
| W-03 | 每局随机洗牌 | 候选词与卡片均经过随机排序，避免相邻局重复感 |

### 4.6 用户中心与学习统计（P1）

| 编号 | 需求 | 说明 |
|---|---|---|
| P-01 | 个人信息卡 | 展示用户名、已掌握词汇数、已完成关卡、累计游戏次数 |
| P-02 | 总体掌握进度 | 进度条 + 文案"已掌握数/词库总数 (百分比)"，百分比封顶 100% |
| P-03 | 分首字母进度 | 按 A–Z 26 组展示已掌握词数与迷你进度条，用于发现薄弱字母 |
| P-04 | 所有用户列表 | 卡片式展示全部本地用户的关卡数与词汇数，支持逐个清除 |
| P-05 | 无用户空态 | 未进行过游戏时提示"请先玩一次游戏以创建用户档案" |

### 4.7 数据持久化（P0）

| 编号 | 需求 | 说明 |
|---|---|---|
| D-01 | 纯本地存储 | 全部数据保存在浏览器 localStorage，无网络请求、无后端 |
| D-02 | 离线可用 | 断网、file:// 双击打开均可正常游戏（词表以 `<script>` 方式随页面加载） |
| D-03 | 对局结束自动保存 | 每局结束时把配对数、得分、关卡、时间写入游戏历史并持久化 |

---

## 5. 核心用户流程

### 5.1 主流程：定向字母练习

```
打开 index.html
  → 弹窗输入用户名（已建档用户自动识别）
  → 字母选择页
     ├─ 不选字母 → 点击"开始游戏" → 全词库随机抽 15 对
     └─ 点击字母 C → 点击"开始游戏"
          → 词库有 C 开头单词：抽词（C 词优先，不足补随机词）→ 进入对局
          → 词库无 C 开头单词：提示换字母，不出牌
  → 对局：90s 内点击配对英文卡与释义卡（+10 分/对）
     ├─ 全部消除 → 烟花 + 通关提示 + 解锁"下一关卡"
     └─ 时间到 → 结算提示，展示累计掌握词数
  → 保存学习数据
```

### 5.2 查看进度流程

```
点击导航"我的"
  → 查看总体进度百分比
  → 查看分字母进度，发现薄弱字母
  → 切回"游戏"页，选择该字母针对性练习
```

### 5.3 多用户流程

```
新用户打开游戏 → 输入新用户名 → 自动建档（超过 3 人时淘汰最旧档案）
"我的"页 → 所有用户列表 → 可清除指定用户数据（confirm 二次确认）
```

---

## 6. 游戏规则与数值表

| 项目 | 数值/规则 |
|---|---|
| 每局卡片数 | 30 张（15 个单词 × 单词/释义各 1 张） |
| 单局时长 | 90 秒 |
| 配对成功得分 | +10 分 |
| 配对失败 | 不扣分，0.5s 后翻回 |
| 通关条件 | 90 秒内消除全部 15 对 |
| 单局满分 | 150 分 |
| 总关卡数 | 10 关，通关后循环回到第 1 关 |
| 网格布局 | 6×5 / 5×6 / 4×8（每局随机） |
| 抽词构成 | 已掌握词 ≤5 + 未掌握词 ≤10，不足再补 |
| 专项练习补词规则 | 目标字母词 <15 时，用其他字母随机词补足 |
| 本地用户上限 | 3 个 |

---

## 7. 界面设计需求

### 7.1 视觉风格

- **整体风格**：低饱和度莫兰迪色系，学术、柔和、护眼，适合长时间学习。
- **主色**：紫色系（主紫 `#9f7aea`、深紫 `#6b46c1/#805ad5`，浅紫底 `#f5f0f8/#f7f3ff`，描边 `#e9d8fd/#d6bcfa`）。
- **辅助色**：选中态暖黄（`#fef3c7/#fbbf24`）、重新开始红色（`#f56565`）、下一关蓝色（`#4299e1`）、继续绿色（`#48bb78`）。
- **质感**：圆角卡片（10–25px）、轻投影、卡片悬浮/选中带位移动效；页面切换淡入动画。
- **卡片配色**：每局从 6 套低饱和度配色（紫、深紫、灰、橙、绿、蓝）中随机统一使用一套。

### 7.2 页面与关键组件

| 页面 | 关键组件 |
|---|---|
| 字母选择页 | 圆形字母按钮 ×26、开始按钮、操作说明文案 |
| 对局页 | 得分胶囊、SVG 圆环倒计时、6 列卡片网格、底部三按钮、提示信息条 |
| 我的页 | 个人信息列表、总体进度条、A–Z 字母进度网格、用户卡片列表、红色清除按钮 |

### 7.3 交互反馈

- 字母按钮：hover 放大变实心，选中保持实心高亮。
- 卡片：hover/选中放大 1.05 倍并加投影；配对成功缩小至 0.95 并变紫；0.3s 过渡动画。
- 倒计时：圆环 stroke-dashoffset 每秒线性变化。
- 通关：游戏区随机生成 20 个彩色粒子向外扩散淡出。

### 7.4 响应式

- 桌面端内容区最大宽度 600px 居中。
- 视口宽度 ≤650px：卡片网格降为 5 列、行高收缩、字号缩小；用户中心容器宽度变为 95%。

---

## 8. 数据需求

### 8.1 词表数据

- 文件：`data/ielts_words.js`，以全局变量 `window.IELTS_WORDS` 暴露，供页面通过 `<script>` 直接加载（保证 file:// 双击离线可用）。
- 结构：

```json
{
  "words": [
    { "word": "abandon", "meaning": "放弃；遗弃" }
  ]
}
```

- 字段约束：`word` 为英文小写单词；`meaning` 为中文释义；单词需去重。
- **现状基线**：当前词库共 **1995 词，26 个首字母全覆盖**。数据来源为 GitHub 开源雅思词表（《雅思词汇词根+联想记忆法》乱序便携版），经去重、按首字母均匀采样（每字母上限 110 词）后合并；X/Y/Z/J/K/Q 等稀疏字母手动补充常见词。各字母词数：A–F/I/M/P/R–T 各 110，G 92、H 106、L 95、N 44、O 98、U 61、V 75、W 45、J 17、K/Q/X/Y 各 8、Z 10。
- 词表扩充工具：`src/parse_words.js`（Node 脚本）可从外部雅思词表 JSON 解析、去重、合并并回写 `ielts_words.js`。

### 8.2 localStorage 数据结构

| 键 | 内容 | 结构 |
|---|---|---|
| `ieltsGameUsers` | 全部用户档案数组（≤3） | `User[]` |
| `currentUser` | 当前用户名（字符串） | `string` |

User 对象：

```json
{
  "name": "用户名",
  "totalMatchedWords": 0,
  "levelsCompleted": 0,
  "masteredWords": ["abandon"],
  "gameHistory": [
    { "date": "ISO 时间字符串", "score": 120, "matchedPairs": 12, "level": 1 }
  ]
}
```

字段说明：
- `totalMatchedWords`：历史累计配对次数（跨局累加，含重复词）。
- `masteredWords`：已掌握的**去重**单词列表，用于抽题与分字母统计。

---

## 9. 技术架构

### 9.1 技术栈

- 原生 HTML5 / CSS3 / JavaScript（ES6+），无任何前端框架与 npm 依赖。
- 数据持久化：localStorage。
- 运行方式：直接双击 HTML（file://）或任意静态 HTTP 服务器均可。

### 9.2 文件结构

```
ielts_game/
├── index.html              # 游戏页（字母选择 + 对局）
├── profile.html            # 用户中心
├── data/
│   └── ielts_words.js      # 词库数据（window.IELTS_WORDS）
└── src/
    ├── game.js             # 游戏主逻辑（IELTSGame 类）
    └── parse_words.js      # 词表扩充/合并的 Node 构建脚本（非运行时依赖）
```

### 9.3 关键实现约束

- 词表必须通过 `<script>` 而非 `fetch` 加载，避免 file:// 协议下本地 JSON 读取被浏览器拦截。
- 词表加载失败不得静默降级为不完整数据，需向用户给出明确错误提示。
- 所有动态 UI（字母、卡片、统计）由 JS 渲染，HTML 只保留容器。

---

## 10. 非功能需求

| 类别 | 要求 |
|---|---|
| 兼容性 | 支持主流现代浏览器（Chrome/Edge/Firefox/Safari 近两年版本）；移动端浏览器可正常浏览与点击 |
| 性能 | 30 张卡片单局无明显卡顿；页面首屏渲染 <1s（本地文件） |
| 离线 | 完全离线可用，不发起任何外部网络请求 |
| 隐私 | 所有数据仅存本机浏览器，不上传、不追踪 |
| 可维护性 | 词表与逻辑分离；扩充词表只需替换数据文件或运行构建脚本 |

---

## 11. 现状差距与后续迭代规划

### 11.1 现状差距（相对产品愿景）

1. ~~词库缺口最大~~ ✅ 已解决：词库已补全至 1995 词、26 字母全覆盖。
2. 分字母进度的进度条分母用词库均值（总数/26）近似，与各字母真实词数不符，字母间百分比不可直接比较。
3. 用户名输入依赖 `prompt` 弹窗，无用户切换入口，多用户场景需清除后重建。
4. 通关判定中"≥10 对通关"为冗余逻辑；时间到即失败，对新手偏难，没有难度曲线。
5. 缺少发音、例句、错词本等记忆增强能力。
6. **数据展示缺乏情感粘性**：当前仅罗列"已掌握 X 词/累计配对 Y 次"等数字，未形成可积累、可视化、用户舍不得丢弃的虚拟资产，禀赋效应未被充分调动，复访与留存动力不足。

### 11.2 迭代路线建议

| 版本 | 主题 | 主要内容 | 状态 |
|---|---|---|---|
| v1.1 | 词库补全 | 导入权威雅思词表，补齐至 1995 词 26 字母全覆盖；修正分字母进度分母为各字母真实词数 | ✅ 已上线 |
| v1.2 | 体验优化 | 用户名改为页内建档/切换；对局增加"剩 X 对"提示；修正通关判定逻辑 | 规划中 |
| v1.3 | 记忆增强 | 单词发音（本地/离线音频优先）、配对成功展示例句；记录易错词并支持错词专练 | 规划中 |
| v1.4 | 难度与激励 | 按掌握度动态调整时长/词数、连续答对加分；学习日历、连续打卡 | 规划中 |
| **v1.5** | **游戏化激励与禀赋效应强化** | **词汇花园、连续打卡、学习日历热力图、成就勋章系统**（详见第 13 章） | **规划中** |
| v1.6 | 多语扩展 | 英文版连连看（英文释义 ↔ 单词配对），复用现有架构 | 规划中 |
| v2.0 | 数据导出 | 支持学习数据 JSON 导出/导入，解决换设备与清缓存丢数据问题 | 规划中 |

---

## 12. 验收标准（当前版本）

1. 双击 `index.html` 可离线进入游戏，浏览器控制台无词表加载错误。
2. 输入用户名后，可选择 A–Z 任一字母开局，30 张卡片正常生成（稀疏字母词不足时自动用其他字母词补足）。
3. 配对成功 +10 分、卡片变匹配态；配对失败自动翻回；全部消除出现烟花并解锁下一关。
4. 倒计时 90s 结束自动结算并写入 localStorage。
5. "我的"页正确显示用户名、已掌握数、已完成关卡、游戏次数、总体进度与 A–Z 分字母进度。
6. 可在"我的"页清除指定用户数据，确认后列表与统计即时更新。

---

## 13. 游戏化激励与禀赋效应强化设计（v1.5）

### 13.1 设计背景与原则

**问题**：当前"我的"页仅展示"已掌握 X 词 / 累计配对 Y 次"等纯数字，用户感知为"记录"而非"拥有"，缺乏情感粘性，复访动力不足。

**灵感来源**：微信"浪漫餐厅"等合成经营类游戏——通过"行为→奖励→可积累的虚拟资产→视觉化成就感"循环，让用户对虚拟资产产生拥有感（禀赋效应），从而舍不得丢弃、持续回访。

**设计原则**（不照搬重度游戏，提炼可在纯前端落地的轻量机制）：

1. **资产可视化**：把抽象的"掌握词数"转化为用户能看见、能拥有的视觉资产（花园/勋章/日历）。
2. **损失厌恶驱动**：连续打卡等机制让用户因"怕中断"而持续行为。
3. **零后端依赖**：全部数据存 localStorage，离线可用，不引入服务端经济系统。
4. **不干扰核心玩法**：激励层是"附加反馈"，不改变连连看配对的核心记忆逻辑。
5. **诚实标注**：所有数据为本地模拟，不涉及真实排行榜或社交比较。

### 13.2 模块一：词汇花园（核心，禀赋效应主载体）

**心理学依据**：禀赋效应（Endowment Effect）——用户对自己"拥有"的东西估值更高、更不愿放弃。把"掌握词数"具象为"我种出来的花园"，让删数据=毁掉花园，大幅提升留存。

**功能描述**：

- 在"我的"页新增一块"我的词汇花园"视觉区，随掌握词数增长而丰富。
- 成长阶梯（阈值为设计参数，可调整）：

| 已掌握词数 | 花园状态 | 视觉元素 |
|---|---|---|
| 0 | 荒地 | 一片棕色土地 + "种下第一颗种子吧" |
| 1–9 | 萌芽 | 1 株小绿芽 🌱 |
| 10–24 | 小花丛 | 多朵小花 🌸 |
| 25–49 | 小花园 | 花 + 第一棵小树 🌳 |
| 50–99 | 繁盛花园 | 花 + 多棵树 + 蝴蝶 🦋 |
| 100–199 | 秘密花园 | 花 + 树 + 小池塘 🐟 |
| 200–499 | 森林花园 | 花园 + 更多树 + 小鸟 🐦 |
| ≥500 | 词汇绿洲 | 完整生态 + 彩虹 🌈 |

- 每次配对成功后，若跨越阈值，弹出"花园升级"庆祝动画。
- 花园用 CSS/emoji 或轻量 SVG 绘制，不引入图片资源（保持离线）。

**数据结构**（追加到 User 对象）：

```json
{
  "gardenStage": 3
}
```

`gardenStage`：整数，对应上述阶梯索引，由 `masteredWords.length` 自动计算，无需单独存储（也可缓存以避免重复计算）。

**验收标准**：
- "我的"页展示词汇花园，且花园状态与当前掌握词数一致。
- 掌握词数跨越阈值时，花园视觉发生可见变化。
- 清除用户数据后花园重置为荒地。

### 13.3 模块二：连续打卡 + 学习日历热力图

**心理学依据**：损失厌恶（Loss Aversion）——用户对"失去连续打卡天数"的痛苦 > "获得一天打卡"的快乐，从而驱动每日回访。日历热力图提供"努力可视化"的正反馈。

**功能描述**：

- **连续打卡天数**：用户每完成一局（无论通关与否），当天记为"已学习"。连续学习天数 +1；若中断（隔天未学习），连续天数归零。
- **学习日历热力图**：在"我的"页展示最近 12 周（84 天）的网格热力图（类似 GitHub contribution graph），每个格子代表一天，颜色深浅表示当天掌握的新词数（0=灰、1–2=浅绿、3–5=中绿、≥6=深绿）。
- 顶部展示"已连续学习 X 天"，中断时变为"上次连续 X 天，再接再厉"。

**数据结构**（追加到 User 对象）：

```json
{
  "streakDays": 3,
  "lastStudyDate": "2026-09-22",
  "dailyStats": {
    "2026-09-20": 5,
    "2026-09-21": 3,
    "2026-09-22": 8
  }
}
```

- `streakDays`：当前连续学习天数。
- `lastStudyDate`：上次学习日期（ISO yyyy-mm-dd），用于判断是否中断。
- `dailyStats`：按日期记录当天新掌握词数（去重后的新增）。

**验收标准**：
- 完成一局后，当天热力图格子颜色加深，连续天数 +1。
- 若当天已学习过，再次开局不重复 +1 连续天数（只更新当天新词数）。
- 隔天未学习后再开局，连续天数重置为 1。
- 热力图正确展示最近 84 天的学习强度。

### 13.4 模块三：成就勋章系统

**心理学依据**：收集欲与成就动机——可量化的里程碑勋章满足用户的收集心理与自我效能感。

**功能描述**：

- 在"我的"页新增"成就勋章"区，展示已解锁与未解锁勋章。
- 勋章设计（阈值为设计参数）：

| 勋章 | 解锁条件 | 图标示意 |
|---|---|---|
| 初出茅庐 | 掌握第 1 个词 | 🌱 |
| 百词斩 | 掌握 100 个词 | 💯 |
| 半壁江山 | 掌握词库 50% | 🏆 |
| 词汇大师 | 掌握词库 100% | 👑 |
| 连击达人 | 连续打卡 7 天 | 🔥 |
| 坚持不懈 | 连续打卡 30 天 | ⭐ |
| 字母专精（A–Z） | 某首字母掌握率 100% | 对应字母徽章 |

- 解锁时弹出勋章获得动画。
- 未解锁勋章置灰显示，提示解锁条件。

**数据结构**（追加到 User 对象）：

```json
{
  "badges": ["first_word", "streak_7"]
}
```

`badges`：已解锁勋章 ID 数组，由系统根据条件自动判定。

**验收标准**：
- 达成解锁条件时，对应勋章从灰色变为彩色，并弹出获得提示。
- 已解锁勋章持久保存，清除数据后重置。
- 字母专精勋章按各字母真实词数计算 100% 分母。

### 13.5 模块间协同与优先级

| 优先级 | 模块 | 理由 |
|---|---|---|
| P0 | 词汇花园 | 禀赋效应主载体，视觉冲击力强，面试展示效果最好 |
| P1 | 连续打卡 + 热力图 | 损失厌恶驱动每日复访，实现简单 |
| P2 | 成就勋章 | 收集欲补充，可与花园/打卡条件复用 |

> 实现顺序建议：先做词汇花园（1–2 天），再做打卡热力图（1 天），最后做勋章系统（1 天）。

---

## 14. 多语扩展：英文版连连看（v1.6）

### 14.1 为什么做

- **复用现有架构**：连连看的配对核心不依赖语言，只需替换"卡面内容"与"配对键"。
- **差异化卖点**：市面上的背词游戏多为"中→英"配对，英文版（英→英）可满足更高阶用户（如已过雅思、备考 GRE/GMAT、或想脱离母语依赖的学习者）。
- **面试加分**：展示"一套架构支撑多语种"的产品复用思维。

### 14.2 功能设计

- 在字母选择页顶部增加语言切换：**中文释义模式（默认）** / **英文释义模式**。
- 中文模式：英文单词卡 ↔ 中文释义卡（当前逻辑）。
- 英文模式：英文单词卡 ↔ 英文释义卡（需词表提供英文释义字段）。
- 两种模式共享用户档案、掌握词列表、花园、打卡等激励层数据（掌握的是"单词"本身，与释义语言无关）。

### 14.3 数据需求

- 词表需扩展 `meaningEn` 字段：

```json
{
  "word": "abandon",
  "meaning": "放弃；遗弃",
  "meaningEn": "to leave completely and finally; forsake"
}
```

- **现状**：当前词表仅含 `meaning`（中文），`meaningEn` 需后续补充（可从 WordNet / 词典 API 获取，或人工录入高频词）。
- 兼容策略：若某词无 `meaningEn`，在英文模式下自动跳过该词或回退显示中文释义（需在 UI 标注）。

### 14.4 复用与改动范围

| 模块 | 是否复用 | 改动说明 |
|---|---|---|
| 配对核心逻辑 | ✅ 复用 | 仅卡片渲染内容从 `meaning` 切换为 `meaningEn` |
| 抽词/混排逻辑 | ✅ 复用 | 无改动 |
| 用户档案/激励层 | ✅ 复用 | 掌握的是单词，与模式无关 |
| 词表数据 | ❌ 需扩展 | 新增 `meaningEn` 字段 |
| UI 切换 | ❌ 新增 | 语言切换 Tab |

### 14.5 验收标准

- 切换至英文模式后，释义卡显示英文释义，配对逻辑正常。
- 无英文释义的单词不出现"undefined"等脏数据，有合理兜底。
- 两种模式下掌握词数、花园状态、打卡数据互通。
